{
  "name": "subreddit-media-viewer",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:lan": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend -- --host 0.0.0.0 --port 5173\"",
    "build": "npm run build -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
